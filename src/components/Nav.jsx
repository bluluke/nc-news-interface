import { Link } from "react-router-dom"
export const Nav = () => {
    return (

        <nav id="navbar">
            <Link to={'/'}>
                <p>Home</p>
             </Link>   
        </nav>
    )
}